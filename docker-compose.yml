version: '3.8'

services:
  db:
    image: postgres:13.1
    env_file:
      - ./.env
    restart: always
    volumes:
      - db_data:/var/lib/postgresql/data/

  backend:
    image: sanya101/food_backend
    env_file:
      - ./.env
    depends_on:
      - db
    restart: always
    volumes:
      - static:/app/static/
      - media:/app/media/

  frontend:
    image: sanya101/food_frontend
    env_file:
      - ./.env
    depends_on:
      - backend
    volumes:
      - frontend_data:/app/build/

  nginx:
    image: nginx:1.22.1
    ports:
      - "8000:80"
    depends_on:
      - frontend
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - static:/var/html/static/
      - media:/var/html/media/
      - frontend_data:/usr/share/nginx/html/

volumes:
  db_data:
  static:
  media:
  frontend_data:
